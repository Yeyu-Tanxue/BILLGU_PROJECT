# ============================================================
# 项目依赖
# ============================================================
# 安装：pip install -r requirements.txt
# ============================================================

# ---- 云端 API ----
openai>=1.0.0          # 大模型网关（OpenAI 兼容接口）
requests>=2.28.0       # ASR REST 调用、通用 HTTP
python-dotenv>=1.0.0   # 从 .env 读密钥

# ---- 数据采集 / 视觉 ----
opencv-python>=4.5.0   # 摄像头采集和图像处理
mediapipe>=0.10.0      # 手部关键点检测
numpy>=1.21.0          # 数值计算

# ---- 开发期工具 ----
pyaudio>=0.2.13        # PC 端麦克风录音（可选；Windows 装不上可用预编译 .whl）
pyserial>=3.5          # 监听 ESP32 串口（serial_recv_recognize.py）
